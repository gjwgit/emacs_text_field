name: emacs_text_field
description: A Flutter TextField widget with common Emacs key bindings.
version: 0.1.3
homepage: https://github.com/gjwgit/emacs_text_field

environment:
  sdk: ^3.11.5
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
